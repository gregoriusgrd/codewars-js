/*
Complete the solution so that it returns true if the first argument(string) passed in ends with the 2nd argument (also a string).

Examples:
Inputs: "abc", "bc"
Output: true

Inputs: "abc", "d"
Output: false

NOT YET COMPLETED
*/

function solution(str, ending){
    const last = test.charAt(test.length - 1)
    const secondLast = test.charAt(test.length - 2)
    const join = secondLast + last
    return join === endi

}

// const test = "abc"
// console.log(solution(test, "bc"))

const test = "abc"
const last = test.charAt(test.length - 1)
const secondLast = test.charAt(test.length - 2)
console.log(last)
console.log(secondLast)
const join = secondLast + last
console.log(join)
